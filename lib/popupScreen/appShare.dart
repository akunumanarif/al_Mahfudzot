import 'package:flutter/material.dart';

class AppShare extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is app Share"),
      ),
    );
  }
}
